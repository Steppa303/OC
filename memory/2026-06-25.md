# 2026-06-25 — Lesestoff Runpod GPU Integration (Fortsetzung)

## Session: Runpod GPU Worker Setup (15:30-16:35)

### Was passiert ist
- Bastian hat mich beauftragt, die runpod.md Doku umzusetzen
- Hab direkt gearbeitet (kein Orchestrator), weil's ein klarer Plan war

### Docker Build (3 Versuche)
1. **Versuch 1:** Models im Docker Build laden → Build hing nach 10+ Min ohne Output → gekillt
2. **Versuch 2:** Models beim Worker-Start laden → Build lief durch (7:30 Min), Image 16.1GB/5.21GB compressed
3. **Optimierung:** `chatterbox-tts==0.1.7` zwingt `torch==2.6.0` (767MB Upgrade von Base 2.5.0) — Constraints halfen nicht, chatterbox forced es. Akzeptiert.

### Änderungen am Code
- **`runpod-worker.py`:** Models dynamisch von Hugging Face laden (kein 5GB Cache im Image), Silero VAD API gefixt
- **`Dockerfile.kartoffelbox-worker`:** Model-Cache entfernt, Base Image `pytorch/pytorch:2.5.0-cuda12.4-cudnn9-runtime`
- **`server.js`:** Runpod API auf V1 umgestellt (`rest.runpod.io/v1`), GPU-Schema korrigiert (`NVIDIA_T4` → `NVIDIA L4`, `containerDiskSizeGb` → `containerDiskInGb`), Template-ID aus `.env`
- **`epub-parser.js`:** `parseContent()` async Wrapper hinzugefügt (fehlte)
- **`requirements-worker.txt`:** `chatterbox-tts==0.1.7`, `silero-vad==6.2.1`, rest aktualisiert

### Runpod Integration
- **Image gepusht:** `ghcr.io/steppa303/lesestoff-worker:latest`
- **Template erstellt:** ID `56eejfcekr`
- **Registry Auth gespeichert:** ID `cmqtls3c3007u8impp3349n3p` (GHCR private → Runpod kann pullen)
- **E2E-Test:** Pod deployed ✅, aber erster Pod failed wegen `IMAGE_AUTH_ERROR` → Registry Auth gefixt, zweiter Pod deployed

### Server Zombie Problem
- PM2 spawnft manchmal doppelt, Ports 3004/3005 bleiben von orphan PIDs belegt
- `kill -9` half nicht immer (PPID 1, init-adoptiert)
- Lösung: `fuser -k 3004/tcp 3005/tcp` + `kill -9` auf die PIDs
- Final: PM2 läuft sauber, 6 Bücher geladen, beide Ports

### Noch offen
- Runpod Pod läuft gerade — warten ob Models geladen werden und Worker completed
- Docker Cache aufräumen (14GB Build Cache, aber `docker builder prune -f` zeigte nur 109KB freed)
- 58GB free auf Platte

### Lessons Learned
- `chatterbox-tts==0.1.7` forced `torch==2.6.0` — kein Weg dran vorbei
- Runpod V1 API hat andere Feldnamen als V2 (`containerDiskInGb` statt `containerDiskSizeGb`, `NVIDIA L4` statt `Tesla T4`)
- GHCR private Images brauchen Registry Auth in Runpod (GraphQL Mutation `saveRegistryAuth`)
- `fuser -k` ist zuverlässiger als `kill -9` für Port-Befreiung
- `docker build` mit `--progress=plain` in Datei umleiten (nicht `| tail`) sonst killt die Pipe den Build
## 20:14 — UI-Freeze-Fix (React Error #520)

**Problem:** Seite komplett eingefroren sobald Bookshelf lädt. Kein Klick, kein Scroll, nichts.
**Root Cause:** `setRunpodStatus()` direkt im Render-Body von `ContextMenuView` → infinite Loop → React Error #520 → ErrorBoundary fängt → re-render → wieder Loop → **Seite tot**
**Fix:** `useEffect` mit Dependencies → einmalig pro Status-Änderung
**Nebenfix:** CORS-Allowlist um `127.0.0.1` erweitert (blockierte Playwright/Headless Chrome)
**Tests:** Playwright bestätigt: Scroll ✅, Menü via Rechtsklick ✅, Scroll mit offenem Menü ✅, Close ✅
**Git:** `e3270bf`

## 21:20 — Runpod Docker Image mit Models gebaut & gepusht

### Was passiert ist
1. `docker system prune -af` → 26GB zurückgeholt
2. Dockerfile aktualisiert: `COPY models-cache-flat /worker/models` hinzugefügt
3. `.dockerignore` angelegt (excludes node_modules, epubs, frontend, etc.)
4. Docker Build gestartet (pytorch base + pip + 5GB Models)
5. Build fertig nach 810s (~13.5 Min) — Image: 8.42GB komprimiert
6. Push zu GHCR: `latest` + `20260625` (SHA: `e6320cada92b`)

### Image-Details
- **Base:** `pytorch/pytorch:2.5.0-cuda12.4-cudnn9-runtime`
- **Packages:** chatterbox-tts==0.1.7, silero-vad, nltk, noisereduce, etc.
- **Models:** `/worker/models/chatterbox/` (3GB) + `/worker/models/kartoffelbox/` (2GB)
- **torch:** 2.6.0 (wird von chatterbox erzwungen, überschreibt base image torch)

### Nächster Schritt
- Runpod Template `56eejfcekr` auf neues Image updaten
- Registry Credentials prüfen (`ghcr-steppa`)
- E2E-Test: Pod starten → Worker bootet → 1 Kapitel rendern

### Lessons Learned
- Build dauert ~13 Min auf VPS (8 Cores, 193GB Disk)
- Export-Phase ist langsam wegen großer Layers (I/O-bound, 47% iowait)
- `docker system prune -af` gibt ~26GB zurück — vor jedem Build machen
