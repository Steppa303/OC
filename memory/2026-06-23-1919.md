# Session: 2026-06-23 19:19:23 GMT+2

- **Session Key**: agent:main:telegram:direct:1400987471
- **Session ID**: 1d5b8bc1-2372-4aef-9611-8e79b16a8ec5
- **Source**: telegram

## Conversation Summary

assistant: Jetzt hab ichs komplett. **ReaderView** befüllt `pages` mit HTML (wenn vorhanden) statt Plaintext. Die TTS-Funktion schickt HTML-Code an Piper → liest Tags vor.

Fix: HTML strippen bevor der Text an TTS geht.


assistant: Jetzt noch checken ob der "full chapter TTS" Flow (nicht page-based) auch sauber ist – der geht direkt über `/api/books/:id/tts`:


assistant: Der Chapter-TTS-Endpoint ist sauber – der holt über `getChapterText()` den Text, den ich schon auf `chapter.text` gefixt hab. Das Problem war nur der page-basierte TTS-Pfad.

Letzter Schritt: Build + Deploy
assistant: Der Server selbst akzeptiert immer noch HTML – da mach ich auch noch einen Safety-Net rein:


assistant: Moment, der Stream muss `cleanText` verwenden, nicht `text`
assistant: Double-checking the final state:


assistant: Deployed. Hier die Zusammenfassung:

---

## ✅ Fix: "Tags werden vorgelesen"

**Ursache:** Zwei unabhängige Pfade konnten HTML an Piper schicken:

1. **Backend (`epub-parser.js`)** – `getChapterText()` returned `chapter.html || chapter.text`. Erstes ist HTML mit `<p>`, `<span>`, `<em>` etc.

2. **Frontend (`ReaderView.tsx`)** – Paginiert `chapter.html` via `paginateHtml()` → die `pages` enthalten HTML-Fragmente → `useTts.ts` schickt die roh an die API.

**Drei Layer gefixt:**

| Layer | Datei | Fix |
|-------|-------|-----|
| Backend (Chapter-TTS) | `epub-parser.js:320` | `return chapter.text` statt `chapter.html \|\| chapter.text` |
| Backend (Chunk-TTS) | `server.js:298-322` | HTML wird auf Serverseite gestrippt + nur `cleanText` an Piper |
| Frontend (Page-TTS) | `useTts.ts:123` | HTML-Tags + Entities werden rausgefiltert, bevor der Text an die API geht |

Damit ist es egal auf welchem Weg der Text zu TTS kommt – **kein HTML-Tag wird mehr vorgelesen**. Und für den Fall dass eine Seite *nur* HTML ohne lesbaren Inhalt hat, wird das vorher abgefangen.

**Alle Stimmen live und deployt:**
- 🇩🇪 Thorsten, Thorsten Emotional, MLS #2422, MLS #4536, MLS #2037, Kerstin, Ramona
- 🇺🇸 Lessac
- ElevenLabs läuft weiter als Alternative
user: Aktualisiere Dokumentation und handover. Dann mach git checkpoint.
assistant: Okay, schreib ich die Doku und mach den Git-Checkpoint.


assistant: 



assistant: Jetzt mach ich die fehlenden Edits einzeln
assistant: 



assistant: Doku steht. Jetzt der Git Checkpoint:


assistant: Commit `ded68ab` – 395 Dateien, 7549 Zeilen Änderungen. Alles drin.


assistant: Kein Remote konfiguriert – der Commit ist lokal. Alles gut.

**Fertig.**

Doku aktualisiert:
- **`piper.md`** – Implementierungs-Doku mit Architektur, Stimmen-Tabelle, gefixten Issues, API, PM2, Dateien
- **`HANDOVER.md`** – Externe Abhängigkeiten (Piper + ffmpeg), API-Erweiterungen, neue Stimmen, Troubleshooting, Entstehungsgeschichte aktualisiert

Git Checkpoint: `ded68ab` – *"Piper TTS: Offline TTS-Alternative mit 8 Stimmen + HTML-Sanitizer"* (395 files, +7549/-70)
