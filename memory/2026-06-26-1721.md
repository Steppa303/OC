# Session: 2026-06-26 17:21:48 GMT+2

- **Session Key**: agent:main:telegram:direct:1400987471
- **Session ID**: 64ea2515-0028-46b6-9e03-eaceae92d163
- **Source**: telegram

## Conversation Summary

user: lesestoff projekt. Checke handover.md

Wir wollen runpod speed optimieren.

Der agent von runpod hat folgendes gesagt:

"Most of the “slowness” in your log comes from two things:
**Your worker is repeatedly re-loading models/VAD/reference audio** (10–12s overhead per job in the timestamps you showed).
The actual render is doing **very expensive sampling** (you have Sampling: …/2048, and you’re doing that across many fragments). 2048 steps per fragment will dominate runtime.
Here are the highest-impact optimizations, in the order I’d tackle them.
**1) Keep models warm (don’t reload per job)**
Your logs show the whole sequence every time:
load Chatterbox base
load fine-tune safetensors
load Silero VAD
load reference WAV
If this is a server/worker pattern, load all of that **once at process start**, then reuse the objects for each request/job. Concretely:
create a singleton “pipeline” object at module import / app startup
only call the TTS inference inside the job handler
keep reference audio cached (or pre-embedded if your pipeline supports it)
If you’re using something like Runpod worker handlers, the module/global scope is typically persistent for the lifetime of the container—so this change usually yields a noticeable speedup.
**2) Reduce sampling steps (2048 is extremely high)**
That progress bar is the smoking gun. If you can reduce steps from:
**2048 → 256** or **128** (or whatever your model tolerates)
you can often get **an order-of-magnitude** speed improvement, depending on quality needs.
If quality degrades, try:
fewer steps + better scheduler
fewer steps + mild guidance tweaks
render at fewer steps for drafts, then re-render only final output"

Schaue in unsure Dokumentation. prüfe den relevanten code gegen und beurteile die Ratschläge vom runpod agent. passe soe ggf. an unsere situation an erstelle einen umsetzumgsplan in einer datei runpod_opt.md

Gebe mir dann im Anschluss hier eine Zusammenfassung
assistant: Jetzt hab ich alles. Lass mich den Plan schreiben.
assistant: Aight, hab alles gecheckt. Datei liegt unter `projects/lesestoff/runpod_opt.md`.

**TL;DR für deine Aufmerksamkeitsspanne:**

Der Runpod-Agent hat **einen kompletten Müll-Vorschlag** gemacht. Sein Tipp mit "2048 Sampling Steps auf 256 reduzieren" ist **falsch für Chatterbox**. Der hat Chatterbox mit Stable Diffusion verwechselt lol. Chatterbox ist **autoregressiv** (GPT-Stil) — die 2048 sind `max_new_tokens` (Längenlimit), keine Qualitäts-Steps. Weniger = kürzeres Audio, nicht schneller. Der hats literally nicht verstanden.

**Was tatsächlich hilft (sortiert nach Impact):**

**🟢 Phase 1 — Quick Wins (kein Quality-Risk):**
1. **Reference Audio cachen** — `prepare_conditionals()` lädt bei JEDEM Fragment die WAV neu + berechnet Embeddings. Einmal cachen = **~2-3 Min pro Kapitel gespart**
2. **Watermarker deaktivieren** — Perth-Watermark ist unnötig für interne Renders. **~30-60s/Kapitel**
3. **max_new_tokens auf 1024** — 2048 ist overkill für 1000-Char-Fragmente, verhindert Halluzination am Ende

**🟡 Phase 2 — Braucht Tests:**
4. **fp16 auf GPU** — Halb-Precision, ~30-50% schnellere Inference auf T4
5. **CFG reduzieren** (0.5 → 0.25 oder 0.0) — spart ~20-40% Compute pro Token (CFG verdoppelt den Forward-Pass)
6. **VAD/NoiseReduce auf CPU verschieben** — GPU soll NUR TTS rechnen, Post-Processing auf VPS

**Erwartung:** 
- Aktuell: ~24h/Buch (~$2.40)
- Nach Phase 1: ~18-19h (~$1.80)
- Nach Phase 2: ~10-12h (~$1.00-1.20)

Phase 1 ist literally 20 Minuten Arbeit. Soll ich direkt umsetzen?
