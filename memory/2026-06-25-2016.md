# Session: 2026-06-25 20:16:13 GMT+2

- **Session Key**: agent:main:telegram:direct:1400987471
- **Session ID**: 17766039-1644-4e30-8074-62865f3ff3c7
- **Source**: telegram

## Conversation Summary

assistant: **403 auf die Assets!** Der Express-Server gibt 403 auf JS/CSS. Check


assistant: Server gibt 200, aber Playwright kriegt 403. Das riecht nach ner Middleware die Headless-Clients blocked. Check


assistant: **Found it.** Playwright öffnet die Seite via `http://127.0.0.1:3004/` – aber die CORS-Allowlist hat nur `http://localhost:3004` und `http://localhost:3000`, nicht `http://127.0.0.1:3004`. Der Browser schickt für JS/CSS-Assets den Origin `http://127.0.0.1:3004` → CORS-Middleware returned 403 → Module kann nicht laden → React mountet nicht → leere Seite.

Fix: `127.0.0.1` in die CORS-Allowlist
assistant: **React mountet!** ✅ Der CORS-Fix hat's gebracht. Jetzt den vollen Test


assistant: Aha! React Error #520 — das ist n **Render-Crash** während des ersten Mounts, der dann vom ErrorBoundary aufgefangen wird, der Re-Render triggert → Crash → Endlosschleife. Das passiert auch im normalen Browser — steht im errorLink. Schauen wir mal
assistant: React Error #520 ist "Rendered more hooks than during the previous render" oder "Max update depth exceeded". Das erklärt den FREEZE — React crasht in ne Endlosschleife. Sehr wahrscheinlich `setRunpodStatus` direkt im Render-Body (statt in useEffect). Check ich schnell
assistant: Die Error-Spam von React #520 ist "Maximum update depth exceeded" — jemand ruft `setState` während des Renders auf. Das ist der Grund warum die Seite einfriert. Schuld ist `setRunpodStatus` direkt im Render-Body. Find ich sofort


assistant: **Gefunden.** In `ContextMenuView` (Zeile 216):
```js
if (runpodStatus) setRunpodStatus(book.id, runpodStatus)
```

Das wird **während des Renders** aufgerufen → updatet Zustand → triggert Re-Render → wieder `setRunpodStatus` → **infinite loop → React Error #520 → App friert ein**. Das Gleiche war vermutlich auch in der alten `BookCard` drin, aber da war `runpodStatus` vielleicht null bis das Menü geöffnet wurde. Anyway — fix: `useEffect` rein
assistant: Wait — `useEffect` fehlt im Import
assistant: Und jetzt hab ich beim Rewrite auch `RunpodBadge` auf `null` gesetzt im Grid — die holen jetzt auch keinen Status mehr beim initialen Rendern (das war vorher in der `BookCard` drin). Fix gebaut, build und test:


assistant: Jetzt test
assistant: **ALLE TESTS BESTANDEN** 🎉

```
1. React gemountet: ✅
2. Scrollen: ✅
3. Bücher gefunden: 6
4. Menü via Rechtsklick: ✅
5. Scroll mit offenem Menü: ✅
6. Menü geschlossen: ✅
```

**Root Cause (endlich gefunden!):**
Nicht CORS, nicht Radix, nicht Portal — **React Error #520** = "Maximum update depth exceeded". Irgendwann in den vorherigen Edits wurde `setRunpodStatus()` direkt im Render-Body aufgerufen (statt im `useEffect`). Das hat ne Endlosschleife ausgelöst: Render → setState → Re-Render → setState → ... → React bricht den Stack → **Seite ist tot**. Kein Klicken, kein Scrollen, nix. Deshalb gings auch im normalen Browser nicht.

**Was jetzt anders ist:**
- `setRunpodStatus` läuft sauber im `useEffect` mit Dependency-Array → einmalig pro Status-Änderung
- Menü ist `fixed`-Overlay im Body → null Interaktion mit Scroll-Container
- `createPortal` rausgeschmissen — einfache Fixed-Overlays, weniger overhead
- CORS-Fix: `127.0.0.1` in Allowlist (hatte Playwright-Tests blockiert)

Build ist live, Server neu gestartet. Probier aus — sollte jetzt flutschen 🫡
user: aktualisiere Dokumentation und handover. dann erstelle git checkpoint.
assistant: Doku updated. Jetzt Git:


assistant: Erledigt. Doku geupdated mit Section 16 (UI-Freeze-Fix inkl. Root-Cause, Fix, Lessons Learned), Git-Commit `e3270bf` mit Description, Daily-Memory aktualisiert.

**Zusammenfassung Commit:**
- `BookshelfGrid.tsx` — `setRunpodStatus` in `useEffect` verschoben
- `server.js` — `127.0.0.1` in CORS-Allowlist
- `HANDOVER.md` — Section 16 + Timestamp geupdated
- Build hash: `index-B6kvzEWc.js`

Sollte jetzt flutschen. Sag Bescheid wenn's immer noch zickt.
