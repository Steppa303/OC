# 26.06.2026

## Lesestoff Runpod E2E-Test

### 07:00 — Image-Push auf GHCR
- `DOCKER_CLIENT_TIMEOUT=600` + `COMPOSE_HTTP_TIMEOUT=600` → Push durch
- Layer von pytorch:2.5.0 waren noch auf GHCR → nur 2 neue Layer (5.3GB Models + 1.15GB pip)
- Tags: `latest` + `20260626` live

### 07:23 — Plans aktualisiert
- `runpod.md` + `runpod-status.md`: Image-Push-Blacker analysiert, 4 Optionen bewertet (3 raus)
- `runsolution.md` gelöscht

### 07:33 — GPU-Fallback + Pod-Start
- server.js: GPU-Fallback auf L4, T4, A40, A5000 erweitert
- Template `58abhgwvj2` mit Registry Auth
- Erster Pod startet mit korrektem Template, kriegt aber keine GPU

### 08:00-09:30 — 5 Pod-Versuche, alle ohne GPU
- **GPU-Kapazitätsproblem:** Runpod hat keine freien Instanzen (L4, T4, A40, A5000, A100, A6000, RTX 4090, RTX 3090 — alle getestet)
- **Ein Pod hat tatsächlich GPU bekommen** (A5000, IP 103.196.86.118) — Models gezogen, Worker started, aber 404 weil Port 3005 während Server-Neustarts down war
- **Bugfix:** `POST /api/internal/runpod/:jobRunId/finished` liest jetzt `error_message` aus Body
- **Lektion:** Server-Zombie-Prozesse killen Internal API — PM2 sauber starten nach kill

### 09:30 — Docs aktualisiert + Git Checkpoint
- HANDOVER.md Sections 14c, 15, 15b aktualisiert
- runpod.md + runpod-status.md auf Stand gebracht