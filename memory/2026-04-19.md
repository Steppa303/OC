# 2026-04-19 - Sunday

## 🚨 Telegram Bot Crashes (Day 3)

**Morning (13:48):** Bot down again – same pattern as yesterday.
- Gateway restart at 13:48, then 13:51, then 13:54
- `bot/close` API call + gateway kill → clean restart
- Telegram working again after ~11 min cooldown
- **Fix worked:** `curl https://api.telegram.org/bot<TOKEN>/close` kills all connections

**Root Cause Pattern (3 days):**
- Gateway gets stuck in polling cycle, no errors logged
- `bot/close` on Telegram API + gateway restart = fix
- Daily restart cron added as permanent solution

**Permanent Fix:**
- Cron: `0 5 * * *` → `openclaw gateway restart` (daily 5 AM refresh)
- Watchdog v6 running (`*/15 * * * *`) monitors gateway health

## 🧹 Maintenance
- **MEMORY.md** created with long-term lessons
- **Git commit:** `3efc581` – "feat: Telegram watchdog v6, MEMORY.md created, Telegram conflict fix documented"
- **Subagents:** None running, all clean
- **ChromaDB:** 0GB, cleanup ran (Sunday 3 AM) ✅

## 🔧 Config Notes
- Gateway systemd service: enabled, auto-starts on boot
- TTS legacy keys migrated via `openclaw doctor --fix`
- Bot: @ogLobster_bot (Bernd), Chat ID: 1400987471

---

_Stand: 20:30. Bot läuft, Watchdog aktiv._
