# 16.07.2026

## Hagebau Partner-Card Registrierung — ERFOLGREICH 🎉

**TL;DR:** Account erstellt, Newsletter aktiviert, aber Partner-Card Antrag liegt im Cross-Origin Iframe — letzter Schritt muss manuell.

### Was passiert ist
- **Xvfb installiert & getestet** → funktioniert einwandfrei auf dem VPS
- **`agent-browser` headed mode** mit `xvfb-run agent-browser --headed` gestartet
- **Cookie-Banner** (Usercentrics) erfolgreich weggeklickt ("Nicht notwendige Cookies ablehnen")
- **Formular ausgefüllt**: Anrede (Herr), Vorname (Bastian), Nachname (Lewin), E-Mail (guenther88@agentmail.to), Passwort (hb#Jungle68)
- **Newsletter-Checkbox** für 10€ Willkommensgutschein aktiviert ✅
- **FriendlyCaptcha** automatisch verifiziert (kein manuelles Eingreifen nötig)
- **REGISTRIEREN** geklickt → Erfolg! → Weiterleitung zu `https://www.hagebau.de/account/`
- **Partner-Card Antrag aufgerufen** (`/partnercard/antrag/`) → Iframe von `loyalty.web.hagebau-connect.io`

### Wichtige Learnings
- **`fill` statt `type`** im agent-browser ist der Gamechanger — `fill` ersetzt den kompletten Input-Wert, `type` appended nur → kein "BastianBastian" mehr
- **Xvfb + headed mode** reicht für die allermeisten Fälle — Vision-Model war nicht nötig
- **Page Transition nach Form-Submit** muss man selbst tracken (kein Auto-Wait)
- **Cross-Origin Iframes** (`loyalty.web.hagebau-connect.io`) sind weiterhin nicht direkt manipulierbar

### Offen
- **Partner-Card Antrag**: Das Iframe-Formular im Antrag muss Bastian selbst ausfüllen (Adresse + Marktwahl + evtl. Captcha). Account ist aber schon da.
- **Vision-Fallback-Config**: Noch nicht in openclaw.json eingebaut — könnte für Zukunft sinnvoll sein.

## Config Verbesserungen (geplant)
- Vision-capable fallback model in openclaw.json (z.B. `openrouter/qwen/qwen3-coder:free` reicht für Screenshot-Analyse)
- `fill` als Standard für Input-Felder nutzen
- Bei Cross-Origin Iframes: User Bescheid sagen statt endlos retryen
# 16.07.2026

## Hagebau Partner-Card Registrierung — ERFOLGREICH 🎉

**TL;DR:** Account erstellt, Newsletter aktiviert, aber Partner-Card Antrag liegt im Cross-Origin Iframe — letzter Schritt muss manuell.

### Was passiert ist
- **Xvfb installiert & getestet** → funktioniert einwandfrei auf dem VPS
- **`agent-browser` headed mode** mit `xvfb-run agent-browser --headed` gestartet
- **Cookie-Banner** (Usercentrics) erfolgreich weggeklickt ("Nicht notwendige Cookies ablehnen")
- **Formular ausgefüllt**: Anrede (Herr), Vorname (Bastian), Nachname (Lewin), E-Mail (guenther88@agentmail.to), Passwort (hb#Jungle68)
- **Newsletter-Checkbox** für 10€ Willkommensgutschein aktiviert ✅
- **FriendlyCaptcha** automatisch verifiziert (kein manuelles Eingreifen nötig)
- **REGISTRIEREN** geklickt → Erfolg! → Weiterleitung zu `https://www.hagebau.de/account/`
- **Partner-Card Antrag aufgerufen** (`/partnercard/antrag/`) → Iframe von `loyalty.web.hagebau-connect.io`

### Wichtige Learnings
- **`fill` statt `type`** im agent-browser ist der Gamechanger — `fill` ersetzt den kompletten Input-Wert, `type` appended nur → kein "BastianBastian" mehr
- **Xvfb + headed mode** reicht für die allermeisten Fälle — Vision-Model war nicht nötig
- **Page Transition nach Form-Submit** muss man selbst tracken (kein Auto-Wait)
- **Cross-Origin Iframes** (`loyalty.web.hagebau-connect.io`) sind weiterhin nicht direkt manipulierbar

### Offen
- **Partner-Card Antrag**: Das Iframe-Formular im Antrag muss Bastian selbst ausfüllen (Adresse + Marktwahl + evtl. Captcha). Account ist aber schon da.
- **Vision-Fallback-Config**: Noch nicht in openclaw.json eingebaut — könnte für Zukunft sinnvoll sein.

## Config Verbesserungen (geplant)
- Vision-capable fallback model in openclaw.json (z.B. `openrouter/qwen/qwen3-coder:free` reicht für Screenshot-Analyse)
- `fill` als Standard für Input-Felder nutzen
- Bei Cross-Origin Iframes: User Bescheid sagen statt endlos retryen

## Vision Model Config Deployed (16.07.2026 21:00)

**Entscheidung:** `nvidia/nemotron-nano-12b-v2-vl:free` als vision-capable model in openclaw.json
**Warum:** Dediziertes VLM (Vision-Language Model), gratis, 131K Context

### Config-Änderungen:
- `openrouter/models` > `nvidia/nemotron-nano-12b-v2-vl:free` hinzugefügt
  - `input: ["text", "image"]`
  - `cost: 0/0` (gratis)
  - `contextWindow: 131072`
  - `maxTokens: 32768`
- `agents.defaults.models` > Alias "Nemotron Nano VL (free)" hinzugefügt

### Validation:
- `openclaw doctor` → keine Config-Fehler (nur pre-existing Xiaomi Warnings)
- Gateway restart ✅ erfolgreich
- Model in Config vorhanden (beim nächsten Provider-Call deployed)

### Probleme:
- OpenRouter free-tier Chat-Completions gehen gerade nicht (401)
- `qwen/qwen2.5-vl-72b-instruct:free` gibt's nicht mehr kostenlos
- `openrouter/free` model hat schon `image` input — könnte als Fallback reichen

### Workflow für Vision-gestütztes Browser-Automation:
1. `agent-browser screenshot` → PNG lokal speichern
2. Vision Model analysiert Bild → "Option X bei Koordinaten (y,z)"
3. `agent-browser click y z` → pixelgenau klicken
4. `agent-browser fill` → Text eingeben

Das hätte beim PrimeNG-Dropdown auf hagebau.de geholfen — Screenshot + Koordinaten-basierter Klick statt Accessibility-Dead-End.

## Hagebau Credentials
- **URL:** https://www.hagebau.de/account/
- **Email:** guenther88@agentmail.to
- **Passwort:** hb#Jungle68
- **Status:** ✅ Registriert, Newsletter aktiv, 10€ Gutschein
- **ToDo:** Partner-Card Antrag im Iframe von loyalty.web.hagebau-connect.io manuell abschließen