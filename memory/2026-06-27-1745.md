# Session: 2026-06-27 17:45:14 GMT+2

- **Session Key**: agent:main:telegram:direct:1400987471
- **Session ID**: c371cd1a-fd1d-414a-8f58-1171feedc7b2
- **Source**: telegram

## Conversation Summary

user: projekt Lesestoff

wenn ich die Bibliothek laden will, steht da "fehler beim laden der bücher". Was ist da los? kannst du das fixen?
assistant: Digga, ich hab's gefunden. Das Problem ist ein Port-Konflikt:

Der **swarmboard-server** (systemd Service) hat sich auf Port 3004 breitgemacht — genau den Port, den der **lesestoff-app** Server braucht. Der lesestoff Server kann gar nicht erst starten (`EADDRINUSE`), weil der Swarmboard schon da sitzt. Resultat: PM2 crash-loopt mit 363 Restarts, und die Caddy reverse proxy für `lesestoff.steppa.online` leitet auf den Swarmboard-Server, der natürlich keine `/api/books`-Endpunkte hat.

Fix: Swarmboard auf Port 3008 umziehen. Mach ich jetzt.
assistant: Alles klar, läuft wieder. Hier die Zusammenfassung:

**Problem:** Der `swarmboard-server` (systemd Service für System-Metriken) hat sich auf **Port 3004** eingenistet — genau dem Port, den der `lesestoff-app` Server braucht. PM2 hat den lesestoff Server 363 Mal neu gestartet und jedes Mal `EADDRINUSE` gekriegt. Die Caddy Reverse Proxy für `lesestoff.steppa.online` hat dadurch Traffic an den Swarmboard-Server geschickt, der natürlich keine `/api/books`-Endpoints hatte → "Fehler beim Laden der Bücher".

**Fix:**
1. **Swarmboard-Server** von Port 3004 → **3008** umgezogen (`/var/www/apps/swarmboard-server/server.js`)
2. systemd Service neu gestartet
3. **lesestoff-app** via pm2 gestartet — läuft sauber auf Port 3004 mit 0 Restarts ✅
4. `/api/books` liefert wieder Daten

Sollte jetzt funktionieren. Lad die Seite mal neu. 🦞
